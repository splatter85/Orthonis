# Current task

Document ID: `orthonis.doc.current-task`.

## OED1: GitHub-first EUTONOS adoption

Status: IN_PROGRESS.

The owner requested deployment of EUTONOS into the newly created Orthonis repository before application implementation. This authorizes initialization and publication of the bounded repository workflow on `main`, not application code, a local installer, paid model calls, Actions dispatch, or changes to the upstream EUTONOS repository.

Source baseline: Orthonis had no branches or commits when inspected. EUTONOS source is pinned to `4eeff6de668a664ce2ebbad249f8eeb8fb21f945` on `splatter85/EUTONOS/main`.

Outcome: populated project guidance, one work owner, source-bound stable references, a small readable NAV, a public-safe historical checkpoint, and runnable repository consistency checks. Deploy the file-based GitHub-facing workflow only; do not claim an installed EUTONOS runtime or native NAV/RAM execution.

Allowed paths in this adoption plan: `README.md`, `AGENTS.md`, `.gitignore`, `eutonos.read.json`, `docs/CURRENT_TASK.md`, `docs/PROJECT.md`, `docs/ARCHITECTURE.md`, `docs/WORKFLOW.md`, `docs/PROJECT_HEALTH.md`, `docs/Experiments.md`, `docs/EUTONOS_ADOPTION.md`, `NAV/README.md`, `RAM/README.md`, `RAM/OED1.md`, `tools/check_repository.py`, and `tests/test_repository.py`. No existing project files need replacement. This public repository must not receive private runtime source, credentials, transcripts, or real machine diagnostics.

Completion criteria: check owner and stable-ID routing, required local links, work selection, exclusions, and negative validator cases; publish using nonforced writes; read the committed result back; record unrun Windows/runtime/independent-agent checks honestly. The installation facts and evidence belong in `docs/EUTONOS_ADOPTION.md`, not a copied upstream installation certificate.

Next action: author and validate the bounded adoption set, then publish and verify exact committed bytes. Current remote state must be rechecked before resuming an interrupted write.
